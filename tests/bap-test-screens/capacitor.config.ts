import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.janaelst.multipleScreens',
  appName: 'bap-test-screens',
  webDir: 'dist',
};

export default config;
